<!--pages/cart/cart.wxml-->
<view >
<bottom-bar totalCount="{{totalCount}}" totalPrice="{{totalPrice}}" selectAll="{{selectAll}}" bind:handleSelectAll="handleSelectAll"/>
<goods-item goodsItems="{{cartItems}}" bind:refreshData="refreshData"/>
</view>
