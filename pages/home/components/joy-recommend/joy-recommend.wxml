<!--pages/home/components/joy-recommend.wxml-->
<view class="recommends">
  <view wx:for="{{recommends}}" wx:key="index">
    <image src="{{item.image}}" bindload="handleImgLoad" class="recommend-img"></image>
    <text class="recommend-text">{{item.title}}</text>
  </view>
</view>
